import styled from "styled-components";

export const FormContainer = styled.div.attrs({
  className:
    "flex items-center justify-center dark:bg-dark2 bg-white rounded-[36px] px-[10px] py-[12px] gap-1",
})``;
